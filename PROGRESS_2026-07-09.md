# mother-embedding — progress summary, 2026-07-09

Continues from `PROGRESS_2026-07-08.md` (project goal, setup, and the
components built so far are described there).

## Resolved: cross-scale strength comparability (issue 2 from yesterday)

`gabor_lift` now divides each modulus by the kernel wavelength λ
(`CreateGaborLifting.jl`). Energy-normalized kernels alone still integrate
more image energy at larger scales — measured max modulus 1.20 / 3.85 / 5.82
at λ=6/12/24 on a real EMNIST "T", roughly proportional to λ. After the
division the per-scale maxima are 0.20 / 0.32 / 0.24, essentially flat.

Effect on T-junctions: the combined top-30 ranking previously contained zero
smallest-scale candidates (scale composition 0/16/14); afterwards it spans
all scales, and the single strongest candidate on the test "T" is the true
junction — a stem point on the vertical stroke pointing up (α=270°) at its
grid neighbor sitting right at the crossbar. (The stem glyph is anchored one
grid step below the junction; that is the detector's geometry, not a
position error.)

## Config changes

- `SCALES = [3, 6, 12, 24]` — 4 scales instead of 3, extended downward
  (a λ=48 kernel would be ~97 px across, larger than the 56 px image).
- `ORIENTATIONS` — 8 orientations at π/8 (22.5°) spacing instead of 4.
  Verified end-to-end that all 8 θ values appear in the Gabor samples.

## T-junction test-notebook improvements

- Glyph geometry: the stem is now drawn from the sampled point to the
  *actual adjacent sampled point* where the crossbar orientation was
  determined (so diagonal stems span the √2× diagonal distance), and the
  crossbar — length equal to the sample spacing at that scale — is centered
  on that adjacent point.
- `TJunctionSample` gained a `phase` field (the stem Gabor sample's phase),
  and both figures now use it as the glyph's hue.
- Saturation encodes strength but is stretched over the *displayed* set
  (weakest shown → 0, strongest → 1) instead of 0 → max, so levels are
  visually distinguishable.
- The per-scale panel figure has a new slider limiting each panel to its
  top-N strongest best-per-location candidates.

## Observation (no action taken)

In the T-junction figures the four diagonal α directions are systematically
weaker than the cardinals (the diagonal neighbor is √2× farther, so its
crossbar sample is farther from the stem): top-30 on the test "T" splits 21
cardinal / 9 diagonal, and the diagonals render pale. This can make the
figures read as "only 4 directions". Note the Gabor orientation count does
not appear in these figures at all — glyph direction is α, the 8 neighbor
compass directions.

## Still to do

Unchanged from yesterday:
1. FPE encoding / mother-embedding construction (`M0` via Fractional Power
   Encoding + bundling).
2. Pair detectors (the learnable relative-offset operators).
3. Classifier + loss.
4. Training loop.
5. Diagnostics/visualization of a trained model.
